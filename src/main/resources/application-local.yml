server:
  port: 7070

spring:
  security:
    oauth2:
      client:
        registration:
          interventions-client:
            client-id: interventions
            client-secret: clientsecret

hmpps.sqs:
  provider: localstack
  queues:
    delius-interventions-events-queue:
      queueName:  delius-interventions-events-queue
      queueAccessKeyId: test
      queueSecretAccessKey: test
      dlqName: delius-interventions-events-dlq
      subscribeTopicId: intervention-events-topic
      subscribeFilter: '{"eventType":[ { "prefix": "intervention." } ] }'
  topics:
    intervention-events-topic:
      arn: arn:aws:sns:${hmpps.sqs.region}:000000000000:intervention-events-local

services:
  hmpps-auth:
    baseurl: http://hmpps-auth:8090/auth
  interventions-api:
    baseurl: http://localhost:8080
